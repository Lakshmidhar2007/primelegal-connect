{
  "entities": {
    "UserAccount": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserAccount",
      "type": "object",
      "description": "Represents a user account within the PRIMELEGAL CONNECT application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user account."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user account.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the user."
        },
        "isLawyer": {
          "type": "boolean",
          "description": "Indicates whether the user is a lawyer or not."
        },
        "registrationDate": {
          "type": "string",
          "description": "Date and time the user registered.",
          "format": "date-time"
        },
        "photoURL": {
          "type": "string",
          "description": "URL of the user's profile picture.",
          "format": "uri"
        },
        "specialty": {
          "type": "string",
          "description": "The lawyer's primary area of legal practice."
        },
        "bio": {
          "type": "string",
          "description": "A professional biography for the lawyer."
        },
        "website": {
          "type": "string",
          "format": "uri",
          "description": "The lawyer's professional website."
        },
        "linkedin": {
          "type": "string",
          "format": "uri",
          "description": "A link to the lawyer's LinkedIn profile."
        },
        "dateOfBirth": {
            "type": "string",
            "format": "date",
            "description": "The lawyer's date of birth."
        },
        "nationality": {
            "type": "string",
            "description": "The lawyer's nationality."
        },
        "nationalityProofUrl": {
            "type": "string",
            "format": "uri",
            "description": "URL to the lawyer's proof of nationality document."
        },
        "barCouncilNumber": {
            "type": "string",
            "description": "The lawyer's Bar Council Registration Number."
        },
        "licenseUrl": {
            "type": "string",
            "format": "uri",
            "description": "URL to the lawyer's license document."
        },
        "cases": {
          "type": "array",
          "description": "An array of cases filed by the user.",
          "items": {
            "$ref": "#/backend/entities/Case"
          }
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "lastName",
        "isLawyer",
        "registrationDate"
      ]
    },
    "PublicLawyerProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PublicLawyerProfile",
      "type": "object",
      "description": "Publicly visible information for a lawyer, derived from their UserAccount.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user this profile belongs to."
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the lawyer."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the lawyer."
        },
        "photoURL": {
          "type": "string",
          "description": "URL of the lawyer's profile picture.",
          "format": "uri"
        },
        "specialty": {
          "type": "string",
          "description": "The lawyer's primary area of legal practice."
        },
        "bio": {
          "type": "string",
          "description": "A professional biography for the lawyer."
        },
        "website": {
          "type": "string",
          "format": "uri",
          "description": "The lawyer's professional website."
        },
        "linkedin": {
          "type": "string",
          "format": "uri",
          "description": "A link to the lawyer's LinkedIn profile."
        },
        "dateOfBirth": {
          "type": "string",
          "format": "date",
          "description": "The lawyer's date of birth."
        },
        "nationality": {
            "type": "string",
            "description": "The lawyer's nationality."
        },
        "barCouncilNumber": {
            "type": "string",
            "description": "The lawyer's Bar Council Registration Number."
        }
      },
      "required": [
        "userId",
        "firstName",
        "lastName"
      ]
    },
    "Case": {
      "title": "Case",
      "type": "object",
      "description": "Represents a legal case submitted by a user.",
      "properties": {
        "caseId": {
          "type": "string",
          "description": "A unique ID for the case."
        },
        "caseSubject": {
          "type": "string",
          "description": "The subject line of the case."
        },
        "documentType": {
          "type": "string",
          "description": "The primary type of document associated with the case."
        },
        "fileName": {
          "type": "string",
          "description": "The name of the uploaded file."
        },
        "notes": {
          "type": "string",
          "description": "Detailed notes or description of the case."
        },
        "submitted": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the case was submitted."
        },
        "status": {
          "type": "string",
          "description": "The current status of the case.",
          "enum": [
            "Submitted",
            "In Review",
            "Expert Assigned",
            "Resolved"
          ]
        }
      },
      "required": [
        "caseId",
        "caseSubject",
        "documentType",
        "submitted",
        "status",
        "fileName"
      ]
    },
    "Appointment": {
      "title": "Appointment",
      "type": "object",
      "description": "Represents an appointment slot created by a lawyer.",
      "properties": {
        "appointmentId": {
          "type": "string",
          "description": "A unique ID for the appointment."
        },
        "lawyerId": {
          "type": "string",
          "description": "The UID of the lawyer who created this slot."
        },
        "dateTime": {
          "type": "string",
          "format": "date-time",
          "description": "The scheduled date and time of the appointment."
        },
        "status": {
          "type": "string",
          "description": "The current status of the appointment.",
          "enum": [
            "Available",
            "Booked"
          ]
        },
        "bookedBy": {
          "type": "string",
          "description": "The UID of the user who booked the appointment."
        }
      },
      "required": [
        "appointmentId",
        "lawyerId",
        "dateTime",
        "status"
      ]
    },
    "Chat": {
      "title": "Chat",
      "type": "object",
      "description": "Represents a chat conversation between a user and a lawyer.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the chat session."
        },
        "participants": {
          "type": "array",
          "items": { "type": "string" },
          "description": "An array containing the UIDs of the two participants (user and lawyer)."
        },
        "lastMessage": {
          "$ref": "#/backend/entities/Message"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["id", "participants", "createdAt"]
    },
    "Message": {
      "title": "Message",
      "type": "object",
      "description": "Represents a single message within a chat.",
      "properties": {
        "id": { "type": "string" },
        "text": { "type": "string" },
        "senderId": { "type": "string" },
        "timestamp": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["id", "text", "senderId", "timestamp"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserAccount",
          "schema": {
            "$ref": "#/backend/entities/UserAccount"
          },
          "description": "Stores user account information. Only the user themselves can read all fields and write to their own document.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user. This is also the Firebase Authentication UID."
            }
          ]
        }
      },
      {
        "path": "/lawyer_profiles/{lawyerId}",
        "definition": {
          "entityName": "PublicLawyerProfile",
          "schema": {
            "$ref": "#/backend/entities/PublicLawyerProfile"
          },
          "description": "Stores public-facing profiles for lawyers. This collection is publicly readable by any user, authenticated or not. Only the lawyer themselves can create or update their own profile.",
          "params": [
            {
              "name": "lawyerId",
              "description": "The unique ID for the lawyer profile, which should be the same as the user's UID."
            }
          ]
        }
      },
      {
        "path": "/appointments/{appointmentId}",
        "definition": {
          "entityName": "Appointment",
          "schema": {
            "$ref": "#/backend/entities/Appointment"
          },
          "description": "Stores appointment slots created by lawyers. Publicly readable. Only the lawyer who created it can write to it. A user can book it by updating the document.",
          "params": [
            {
              "name": "appointmentId",
              "description": "A unique ID for the appointment slot."
            }
          ]
        }
      },
       {
        "path": "/chats/{chatId}/messages/{messageId}",
        "definition": {
          "entityName": "Message",
          "schema": { "$ref": "#/backend/entities/Message" },
          "description": "Stores messages for a specific chat. Only participants of the chat can read and write messages.",
          "params": [
            { "name": "chatId", "description": "The unique ID of the chat." },
            { "name": "messageId", "description": "The unique ID of the message." }
          ]
        }
      },
      {
        "path": "/chats/{chatId}",
        "definition": {
          "entityName": "Chat",
          "schema": { "$ref": "#/backend/entities/Chat" },
          "description": "Metadata for each chat. Read/write access is limited to participants.",
          "params": [
            { "name": "chatId", "description": "The unique ID of the chat." }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to support user authentication, case submissions, public lawyer profiles, and now real-time chat. The `chats` collection stores metadata, and each chat has a subcollection for `messages`. This is a common and scalable pattern for chat applications in Firestore."
  }
}
